You are working on the existing wellness-connect-app.

Current app understanding:
- This is currently a frontend-only React + Vite prototype.
- Most app logic is inside src/app/App.tsx.
- Data is mocked in src/app/data/mockData.ts.
- Styling is mostly custom CSS in src/styles/wellnessconnect.css, with design tokens in src/styles/theme.css.
- The app direction is a wellness platform combining counselling + fitness training.
- Existing routes include public pages, authenticated shell, client, counsellor, trainer, help desk, admin, appointments, programs, progress, messages, and profile.

Now convert this project toward production architecture.

Technology decision:
- Frontend must remain React + Vite.
- Backend must be PHP Laravel.
- Database must be MariaDB.
- Do not use Node/Express backend.
- Do not use Firebase/Supabase.
- Replace mocked workflows gradually with Laravel API-driven data.
- Keep current UI design as much as possible while refactoring safely.

First production feature to implement:
Admin / System Owner module.

Admin responsibilities to implement first:

1. User Management
- View all users
- Create user
- Edit user
- Disable/delete user
- Assign roles
- Create/edit/delete roles
- Approve counsellors
- Approve trainers
- View approval status

2. System Control
- Configure basic workflows
- Manage permissions using RBAC
- Create permissions
- Assign permissions to roles
- Protect Laravel API routes based on permissions
- Protect React routes/screens based on logged-in user role/permissions

3. Analytics
- Revenue reports page
- Usage metrics page
- Performance dashboard page
- Use mock-seeded database data initially, but fetch via Laravel API, not frontend mockData.ts
- Show cards/charts/tables suitable for admin dashboard

4. Operations
- Platform health page
- Show basic system status:
  - API status
  - database status
  - active users count
  - pending approvals count
  - open escalations count
- Escalation management:
  - list escalations
  - view escalation detail
  - update status
  - assign escalation owner

5. Content Control
- Manage wellness programs/content
- Create/edit/delete programs
- Program fields:
  - title
  - category: counselling / fitness / combined
  - description
  - status: draft / published / archived
  - created_by
  - updated_by

Backend Laravel requirements:
- Create a Laravel backend folder if not present, preferably /backend.
- Configure MariaDB connection through .env.
- Create migrations, models, seeders, controllers, API routes, validation requests, and policies/middleware as needed.
- Use Laravel Sanctum for authentication.
- Implement role-based access control tables manually or using a clean Laravel-compatible RBAC structure.
- Required core tables:
  - users
  - roles
  - permissions
  - role_user
  - permission_role
  - professional_profiles
  - approval_requests
  - workflows
  - workflow_steps
  - programs
  - escalations
  - revenue_reports or payments mock table
  - usage_metrics
  - system_health_logs

Suggested user roles:
- admin
- client
- counsellor
- trainer
- helpdesk
- finance
- legal
- content_manager

Minimum permissions:
- users.view
- users.create
- users.update
- users.delete
- roles.manage
- permissions.manage
- approvals.view
- approvals.approve
- approvals.reject
- workflows.manage
- analytics.view
- platform_health.view
- escalations.view
- escalations.update
- programs.view
- programs.create
- programs.update
- programs.delete
- programs.publish

Laravel API endpoints to create:
Authentication:
- POST /api/login
- POST /api/logout
- GET /api/me

Admin users:
- GET /api/admin/users
- POST /api/admin/users
- GET /api/admin/users/{id}
- PUT /api/admin/users/{id}
- DELETE /api/admin/users/{id}

Roles and permissions:
- GET /api/admin/roles
- POST /api/admin/roles
- PUT /api/admin/roles/{id}
- DELETE /api/admin/roles/{id}
- GET /api/admin/permissions
- POST /api/admin/roles/{id}/permissions

Approvals:
- GET /api/admin/approvals
- POST /api/admin/approvals/{id}/approve
- POST /api/admin/approvals/{id}/reject

Workflows:
- GET /api/admin/workflows
- POST /api/admin/workflows
- PUT /api/admin/workflows/{id}
- DELETE /api/admin/workflows/{id}

Analytics:
- GET /api/admin/analytics/revenue
- GET /api/admin/analytics/usage
- GET /api/admin/analytics/performance

Operations:
- GET /api/admin/system-health
- GET /api/admin/escalations
- GET /api/admin/escalations/{id}
- PUT /api/admin/escalations/{id}

Programs/content:
- GET /api/admin/programs
- POST /api/admin/programs
- GET /api/admin/programs/{id}
- PUT /api/admin/programs/{id}
- DELETE /api/admin/programs/{id}
- POST /api/admin/programs/{id}/publish
- POST /api/admin/programs/{id}/archive

Frontend React requirements:
- Keep React + Vite.
- Create a clean API client layer:
  - src/app/api/client.ts
  - src/app/api/authApi.ts
  - src/app/api/adminApi.ts
- Move admin screens out of App.tsx into modular files.
- Suggested folder structure:
  - src/app/features/admin/AdminDashboard.tsx
  - src/app/features/admin/users/AdminUsersPage.tsx
  - src/app/features/admin/roles/AdminRolesPage.tsx
  - src/app/features/admin/approvals/AdminApprovalsPage.tsx
  - src/app/features/admin/workflows/AdminWorkflowsPage.tsx
  - src/app/features/admin/analytics/AdminAnalyticsPage.tsx
  - src/app/features/admin/operations/AdminOperationsPage.tsx
  - src/app/features/admin/programs/AdminProgramsPage.tsx
  - src/app/features/auth/AuthProvider.tsx
  - src/app/features/auth/RequireAuth.tsx
  - src/app/features/auth/RequirePermission.tsx

Admin UI pages to create:
1. Admin Dashboard
- Summary cards:
  - total users
  - pending counsellor approvals
  - pending trainer approvals
  - monthly revenue
  - active programs
  - open escalations
- Recent activity table
- Quick actions

2. User Management Page
- Users table
- Search/filter by role/status
- Create/edit user drawer or modal
- Assign role
- Disable/delete action

3. Roles & Permissions Page
- Roles list
- Permissions matrix
- Create/edit role
- Assign permissions to role

4. Approvals Page
- Pending counsellor/trainer approvals
- Profile details
- Documents/status placeholder
- Approve/reject actions

5. Workflow Configuration Page
- Workflow list
- Workflow steps
- Enable/disable workflow
- Edit step names/order

6. Analytics Page
- Revenue reports
- Usage metrics
- Performance dashboard
- Use seeded backend data

7. Operations Page
- Platform health cards
- Escalations table
- Escalation status update

8. Programs / Content Page
- Program list
- Create/edit program
- Publish/archive program

Important implementation rules:
- Do not break existing landing/login/signup UI.
- Do not remove existing mocked pages unless replaced safely.
- First create backend foundation and seed admin data.
- Then connect only Admin module to real Laravel APIs.
- Other roles can continue using mockData temporarily.
- Fix obvious encoding/mojibake issues like Todayâ€™s while touching files.
- Use environment variables:
  - VITE_API_BASE_URL=http://localhost:8000/api
- Provide clear README instructions for:
  - Laravel setup
  - MariaDB setup
  - migrations
  - seeders
  - running backend
  - running frontend

Expected output:
- Working Laravel backend with MariaDB migrations and seeders.
- Working React Admin module connected to Laravel APIs.
- Admin login should show real API-driven admin dashboard.
- RBAC should protect admin endpoints and frontend admin routes.
- Code should be modular, readable, and production-ready enough for future expansion. 

Additional implementation instructions for wellness-connect-app:

1. Remove current design-system dependency

- Remove the current custom/token-based design system from active usage.
- Do not continue depending on:
  - src/styles/theme.css
  - TOKENS_ARCHITECTURE.md
  - old token architecture
  - current custom design-system foundation
- Keep files only if needed for reference, but do not use them in the new implementation.
- New UI should be built using Tailwind CSS utility classes only.
- Use clean Tailwind-based layouts, cards, tables, forms, modals, dashboards, badges, and sidebars.
- Do not introduce a heavy custom CSS system again.
- Do not create a new design-token system now.
- Later we will provide a Tailwind-compatible design system separately.

Frontend styling rules:
- Use Tailwind CSS as the primary styling approach.
- Avoid handcrafted page-specific CSS unless absolutely necessary.
- Prefer reusable React components styled with Tailwind.
- Use simple, professional, wellness-focused UI.
- Keep the UI responsive.
- Use accessible buttons, labels, form fields, tables, and dialogs.

Images:
- Use only open-source/free-to-use images from Unsplash.
- Do not use copyrighted images.
- Use remote Unsplash image URLs only for now.
- Keep image usage minimal and relevant to wellness, counselling, fitness, health coaching, meditation, and personal training.
- Add fallback UI if an image fails to load.

2. Docker setup for Laravel + MariaDB

Use Docker for the backend environment.

Important:
- I already have Docker Desktop installed.
- I already have PHP, MariaDB, and backend-related Docker images available in Docker Desktop.
- If possible, reuse existing suitable local images.
- If reuse is not reliable, create a clean docker-compose setup for this app.
- Use a new database specifically for this wellness app.
- Do not mix this app’s database with any existing project database.

Required Docker services:
- backend / php-laravel
- mariadb
- phpmyadmin optional but preferred

Suggested database:
- DB_DATABASE=wellness_connect_db
- DB_USERNAME=wellness_user
- DB_PASSWORD=wellness_password
- DB_ROOT_PASSWORD=root_password

Suggested ports:
- Laravel backend: http://localhost:8000
- MariaDB: 3307 or another free port if 3306 is already used
- phpMyAdmin: http://localhost:8082 or another free port if 8081 is already used

Create or update:
- docker-compose.yml
- backend/Dockerfile if required
- backend/.env.example
- README setup instructions

Laravel .env should use Docker service name for DB host:
DB_CONNECTION=mysql
DB_HOST=mariadb
DB_PORT=3306
DB_DATABASE=wellness_connect_db
DB_USERNAME=wellness_user
DB_PASSWORD=wellness_password

Frontend .env:
VITE_API_BASE_URL=http://localhost:8000/api

Docker requirements:
- Backend container should install required PHP extensions for Laravel and MariaDB.
- Composer install should work inside the container.
- Laravel artisan commands should run inside the backend container.
- Migrations and seeders should create all required admin/RBAC tables.
- Seed default admin user.

Default seeded admin:
Email: admin@wellnessconnect.local
Password: password

Development commands to document:
- docker compose up -d --build
- docker compose exec backend composer install
- docker compose exec backend php artisan key:generate
- docker compose exec backend php artisan migrate:fresh --seed
- docker compose exec backend php artisan serve --host=0.0.0.0 --port=8000

Implementation priority:
1. Set up Dockerized Laravel + MariaDB backend.
2. Create new MariaDB database for this app.
3. Implement Laravel Sanctum authentication.
4. Implement Admin RBAC APIs.
5. Refactor frontend Admin module to use API.
6. Replace active styling with Tailwind-only UI.
7. Use Unsplash open-source images only where needed.

Do not overbuild the design system now.
The goal is a clean, working, full-stack Admin module with Tailwind UI and Dockerized Laravel/MariaDB backend.